language: node_js
node_js:
  - lts/*
install:
  - npm ci
script:
  - npm install -g yarn
  - yarn build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: dist
  # A token generated on GitHub allowing Travis to push code on you repository.
  # Set in the Travis settings page of your repository, as a secure variable.
  github_token: "6-s5Pv10gVRBkXZfNzukfg"
  keep_history: true
  on:
    branch: main
